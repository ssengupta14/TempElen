
Reset root password			mysqld --init-file=C:\\Ashwini\\ElenVerve\\webproject\\TempElen\\SQL\\resetRootPass.txt
connect mysql				mysql -u root -p	
db changed					used mysql
INSERT INTO user (host, user, password, select_priv, insert_priv, update_priv) VALUES ('localhost', 'guest', PASSWORD('guest'), 'Y', 'Y', 'Y');

CREATE DATABASE ev;

CREATE TABLE USER_DTL (
		USER_ID BIGINT NOT NULL,
		F_NAME VARCHAR(20),
		m_NAME VARCHAR(20),
		L_NAME VARCHAR(20) NOT NULL,
		GENDER VARCHAR(1),
		EMAIL_ID VARCHAR(25),
		PHONE  VARCHAR(15) 	,
		USER_VERIFIED INT,
		V_EMAIL_SENT_DATE DATE	,
		USER_TYPE INT,
	PRIMARY KEY (USER_ID)		
);

CREATE TABLE USER_CREDT (   
	USERNAME VARCHAR(10) NOT NULL,   
	PASSWORD VARCHAR(32) NOT NULL, 
	USER_ID BIGINT,
	PRIMARY KEY (USERNAME) ,
	FOREIGN KEY (USER_ID) REFERENCES USER_DTL(USER_ID) 
); 

CREATE TABLE USER_AUTH (  
	USERNAME VARCHAR(10) NOT NULL,  
	AUTHORITY VARCHAR(10) NOT NULL,  
	FOREIGN KEY (USERNAME) REFERENCES USER_CREDT(USERNAME)  
); 
	
CREATE TABLE ADDRESS_DTL(
		USER_ID BIGINT,
		ADD_NAME VARCHAR(50),
		STREET_NUMBER VARCHAR(10),
		STREET_NAME VARCHAR(20),
		APT_NO VARCHAR(10),
		CITY VARCHAR(20),
		ZIP VARCHAR(15),
		COUNTRY VARCHAR(3),
		ADD_TYPE INT,
		FOREIGN KEY (USER_ID) REFERENCES USER_DTL(USER_ID) 
	)	;
	
CREATE TABLE FRD_CHK_DTL(
	USER_ID BIGINT,
	IP_ADDRESS VARCHAR(24),	
	BRWS_CONFIG VARCHAR(255),
	LAST_LOGGED_IN_DT DATE,
	LAST_PURCHASED_DT DATE,
	LAST_LOGOUT_DT DATE,	
	FRD_ACC_FLG INT,
	LAT VARCHAR(20),
	LONGITUDE VARCHAR(20),
	COUNTRY VARCHAR(3),
	FOREIGN KEY (USER_ID) REFERENCES USER_DTL(USER_ID)
);

CREATE TABLE FRD_CHK_DTL_HS(
	USER_ID BIGINT,
	IP_ADDRESS VARCHAR(24),	
	BRWS_CONFIG VARCHAR(255),
	LAST_LOGGED_IN_DT DATE,
	LAST_PURCHASED_DT DATE,
	LAST_LOGOUT_DT DATE,
	FRD_ACC_FLG INT,
	LAT VARCHAR(20),
	LONGITUDE VARCHAR(20),
	COUNTRY VARCHAR(3)
)
------------------------DID NOT WORK ON BELOW ABLES -----------------------------------------
CREATE TABLE SOCIAL_DTL(
		SCL_ID INT,
		USER_NM VARCHAR(100),
		SCL_TYP_ID INT,
		AUTH_TOKEN VARCHAR(1000)  ---  BLOB
)


CREATE TABLE PRODUCT_DTL(
	PROD_ID VARCHAR(25),
	PROD_CAT_ID VARCHAR(10),
	PROD_SCL_ID(10),
	PROD_IMG_ID VARCHAR(20)
	PROMO_OFFER_ID VARCHAR(10),	
	PROD_NAME VARCHAR(25),
	PROD_DESC VARCHAR(500),
	ORIGINAL_PRICE FLOAT (3,3)
	FINAL_PRICE FLOAT(3,3),
	OO_STOCK_IDX INT,
	NEW_IND VARCHAR(1)
	)
			
CREATE TABLE PROD_SCL_DTL(
	PROD_SCL_ID INT,
	TESTIMONIAL_ID INT,
	LIKES_CTR INT,
	RECOMENDATION_ID INT,
	TWEETS_CTR INT,
	SHARED_CTR INT
	)

CREATE TABLE TESTIMONIAL_DTL(
	TESTIMONIAL_ID INT,
	USER_NAME VARCHAR(25),	
	DETAILS VARCHAR(100)
)	


CREATE TABLE PROD_CATEGORY_ID (
	PROD_CATEGORY_ID INT,	
	COLL_ID INT,
	CATEGORY_NAME VARCHAR(20)
	)


CREATE TABLE COLLECTION_DTL(
	COLL_ID INT,
	DES_IDINT,
	COLLECTION_NAME VARCHAR(20)
	)


CREATE TABLE DESIGNER_DTL(
	DESIGNER_ID INT,
	CATEGORY_NAME VARCHAR(20)
	)

CREATE TABLE PROD_IMG_DTL
	PROD_IMG_ID,
	PROD_IMG_1 VARCHAR(20),
	PROD_IMG_2 VARCHAR(20),
	PROD_IMG_3 VARCHAR(20),
	PROD_IMG_4 VARCHAR(20),
	PROD_IMG_5 VARCHAR(20),
	PROD_VID   VARCHAR(20)
	)



CREATE TABLE PCHASE_HISTORY_DTL(
	CUST_ID INT ,
	PROD_IDBIGINT,
	LAST_PURCHASED_DT DATE
	)

CREATE TABLE PROMO_OFFER_DTL
	PROMO_OFFR INT,
	FREE_SHIPPING_DTL VARCHAR(100)	,
	DISC_ID INT ,
	BAGF_ID INT ,
	PROMO_TYPE VARCHAR(20),
	PROMO_DESC VARCHAR(100)
	)


CREATE TABLE DISCOUNT_DTL
	DICS_ID INT,
	DISC_TYPE VARCHAR(20),
	DISC VARCHAR(100),	
	DISC_PERC VARCHAR(100)
	)


CREATE TABLE FREE_SHIPPING_DTL(
	FREE_SHIPPING_ID INT,
	QUALIFY INT,
	MIN_ORDER_AMT FLOAT(3,3)
	)


CREATE TABLE BUY_GET_FREE_DTL (
	BAGF_ID INT,
	BUY_QTY INT,
	GET_QTY	INT,
	BAGF_TYPE VARCHAR(50),
	BAGF_DESC VARCHAR(100)
	)
		
	
	